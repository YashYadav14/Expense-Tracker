# ⚠️ IMPORTANT: Never commit actual .env file to version control!
# This file is for REFERENCE ONLY - it contains placeholder values
# Copy this to .env and fill in YOUR actual values

# Server Configuration
PORT=5000

# MongoDB Configuration
# For LOCAL DEVELOPMENT (MongoDB installed locally):
# MONGODB_URI=mongodb://localhost:27017/expense-tracker

# For MONGODB ATLAS (Cloud - Recommended):
# Get your connection string from https://cloud.mongodb.com
# Replace the placeholder below with your actual connection string
# MONGODB_URI=<PASTE-YOUR-MONGODB-ATLAS-URL-HERE>

# For local testing:
MONGODB_URI=mongodb://localhost:27017/expense-tracker

# JWT Secret Key
# ⚠️ CRITICAL: Use a strong, random string in production
# Generate a new one using:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 
# Examples of STRONG secrets (use your own generated one):
# - At least 32 characters
# - Mix of uppercase, lowercase, numbers, and special characters
# - Unique per environment
#
JWT_SECRET=<GENERATE-YOUR-OWN-SECRET-KEY>

# Node Environment
NODE_ENV=development
# Use: development, staging, production

