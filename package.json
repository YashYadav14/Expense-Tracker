{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Full-stack expense tracking application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server start\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "preview": "wrangler pages dev client/dist"
  },
  "keywords": [
    "expense",
    "tracker",
    "react",
    "node",
    "mongodb"
  ],
  "author": "YashYadav",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wrangler": "^4.59.2"
  }
}
