name = "expense-tracker-frontend"
type = "javascript"
compatibility_date = "2024-01-01"

# Build configuration
[build]
command = "cd client && npm run build"
cwd = "./"

# Site configuration for static files
[site]
bucket = "./client/dist"

# Environment variables
[env.production]
name = "expense-tracker-production"

# Routes (optional - add your domain here)
# routes = [
#   { pattern = "example.com/*", zone_id = "your-zone-id" }
# ]
